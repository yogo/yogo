#content-title
  = @role.name
  #subtitle Role Settings
#content-actions
  = link_to("Delete", role_path(@role), :class => 'icon icon-delete', :method => :delete, 
    :confirm => "Are you sure you want to delete the #{@role.name} role?\nContinue?" )
= clear_break
= render(:partial => 'form')
