.small-centered.boxed
  %h1#content-title
    #subtitle Create a Project
    
.small-centered
  .rbox.dashed
    - form_for(:project, @project, :url => yogo_projects_path, :builder => YogoFormBuilder) do |f|
      = f.text_field :name, :label => "Project Name:" 
      = f.text_area(:description, :label => "Project Description: ")
      = f.submit('Save', :class => 'save-link')
      = link_to('Cancel', root_path, :class => 'cancel-link') 
