
#content-block
  %table
    %thead
      %tr
        %th User Name
        - for group in @groups
          %th= group.name
    %tbody
      - for user in @users
        %tr
          %td= user.login
          -for group in @groups
            %td
              -if user.groups.include?(group)
                In Group
              -else
                NOt in group