<iframe id="editor-frame" width="846" height="400"></iframe>

<script>
    var baseUrl = '/projects';
    var projectId = <%= @project.id %>;
    var editor_frame = $('iframe#editor-frame');
    editor_frame.attr('src', '/model_editor.html');
    editor_frame.load(function() {
        console.debug(this);
        this.contentWindow.ModelEditor.mainDataLoad(baseUrl, projectId);
    });
</script>