-form_for(@group, :builder => YogoFormBuilder) do |f|
  = f.text_field(:name, :label => "Name:" )
  = f.text_area(:description, :label => "Description:")
  = f.submit('Save', :class => 'button-link')
